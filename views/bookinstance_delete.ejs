<!DOCTYPE html>
<html>
  <head>
    <%-include('./partial/head.ejs') %>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-2"><%-include('./partial/sidenav.ejs') %></div>
        <div class="col-sm-10">
          <h1><%= title %> :<%= bookinstance.book.title %></h1>
          <p>
            <strong>ID: </strong>
            <%= bookinstance.book._id %>
          </p>
          <p>
            Are you sure you want to delete
            <strong><%= bookinstance.book.title %> </strong>
          </p>
          <form action="" method="post">
            <input
              type="hidden"
              name="bookinstanceid"
              id="bookinstanceid"
              value="<%= bookinstance._id %>"
            />
            <button class="btn btn-primary" type="submit">Delete</button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
